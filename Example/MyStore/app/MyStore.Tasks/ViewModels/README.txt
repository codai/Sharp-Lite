This folder holds the view model objects specific to the root Domain namespace.  
Subnames each of their own ViewModels folder.

Alternatively, you could put all the view model objects within a single, root 
"ViewModels" folder or even put them into a separate assembly for strong separation.