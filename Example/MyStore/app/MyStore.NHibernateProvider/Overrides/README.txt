This folder contains any override-related classes for the domain classes.

For larger projects, you may want to organize overrides to reflect the domain's namespace breakdown; e.g.,:

/Overrides
/ProductMgmt/Overrides

When starting a project, agree as a team how you'll organize these folders.